# Astrology Microservice (Unused)

This Python FastAPI service is **not currently used** by the main application.

Western astrology calculations are handled directly in the Node.js backend via `swisseph` (`backend/src/services/astrology.ts`).

This directory is preserved for potential future use as a standalone astrology microservice.
