# Legacy Prototype

This directory contains the original SoulAI React prototype. It is **not the active codebase**.

**Active codebase:** `frontend/` + `backend/`

This prototype is preserved for reference only. Do not make changes here.
